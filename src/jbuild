(library
 ((name erlsub)
  (public_name erlsub)
  (libraries (base))
  (preprocess (pps (ppx_compare)))))

(rule
 ((targets (lexer.ml))
  (deps    (lexer.mll))
  (action  "ocamllex ${<}")))

(rule
 ((targets (parser.ml parser.mli))
  (deps    (parser.mly))
  (action  "menhir ${<}")))
